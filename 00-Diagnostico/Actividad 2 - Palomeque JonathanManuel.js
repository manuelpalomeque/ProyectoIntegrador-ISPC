// 1. Desestructuración 
const { nombre, edad, profesion } = persona;
console.log(`${nombre} tiene ${edad} años y trabaja como ${profesion}.`);

// 2. Agregar nueva propiedad "ciudad"
persona.ciudad = "Rosario";